<!DOCTYPE html>

<html>
<head>
    <meta charset="utf-8" name="viewport" content="width= device- width, innitial-scale=1" />
    <title>@ViewBag.Title</title>
    <!-- Latest compiled and minified CSS -->
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">

    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>

    <!-- Latest compiled JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>



    <style type="text/css">
        .imgbook {
            transition: all 1s ease-in;
            width: 400px;
        }

            .imgbook:hover {
                transform: scale(0.9);
                cursor: pointer;
            }

        .imgsachbannhieu {
            width: 100px;
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>

<body>






    <nav class="navbar navbar-inverse">
        <!--navbar-fixed-top-->
        <div class="container">

            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navAdmin" aria-expanded="false">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="@Url.Action("Home","Home")">
                    SachOnlineAdmin
                </a>
               
               
            </div>
           
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="navAdmin">
                <ul class="nav navbar-nav">
                    <li><a href="@Url.Action("Index","SachOnline")">QUẢN LÝ SÁCH</a> </li>
                    <li><a href="@Url.Action("Index","ChuDe")">QUẢN LÝ CHỦ ĐỀ</a> </li>
                    <li>
                        <a href="@Url.Action("Index","NhaXuatBan")">
                            QUẢN LÝ NHÀ XUẤT BẢN
                        </a>
                    </li>
                    <li>
                        <a href="@Url.Action("Index","KhachHang")">
                            QUẢN LÝ KHÁCH HÀNG
                        </a>
                    </li>
                    <li>
                        <a href="@Url.Action("Index","DonHang")">QUẢN LÝ ĐƠN HÀNG</a>
                    </li>
                    <li>
                        <a href="@Url.Action("Index","Menu")">QUẢN LÝ MENU</a>
                    </li>
                    <li>
                        <a href="@Url.Action("Index","TrangTin")">QUẢN LÝ TRANG TIN</a>
                    </li>
                </ul>
                @{ var ad = Session["Admin"] as NguyenQuocNhut_SachOnline.Models.ADMIN;}
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        @if (ad != null)
                        {

                        <li class="active"><a href="/">Xin Chào : @ad.HoTen</a> </li>
                        <li class="active"><a href="@Url.Action("Logout", "Home")">Đăng xuất</a> </li>
                    }
                    </ul>
                


                </div>

            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container-->
    </nav>

    <div class="container">
        @RenderBody()
    </div>
    <footer class="container well">
        <div style="text-align:center;">
            <p>
                ĐÂY LÀ TRANG QUẢN LÍ CỦA ADMIN<br />
                Email: 2124802010364@student.tdmu.edu.vn
            </p>
        </div>
    </footer>
   

</body>
</html>
